- name: This is our first play
  hosts: webserver1
  tasks:
    - name: "Run a test script on webserver"
      script: test_script.sh Mohit creates=/tmp/mohit.txt

# In this script module will run only when mohit.txt file is not there on the target machine in /tmp

